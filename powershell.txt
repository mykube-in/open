# to know data type
$superSecretObject.PrimaryKey.GetType().Name # to know data type 

# to display Value
$superSecretObject.PrimaryKey

az ad sp list --filter "displayName eq 'anynamehere'" --query '[0].objectId' -o tsv
